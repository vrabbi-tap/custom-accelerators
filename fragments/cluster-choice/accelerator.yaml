accelerator:
  options:
  - name: cluster
    inputType: select
    label: Inner Loop Cluster
    choices:
    - value: "kind-tce-tap"
      text: "Local TAP on TCE Cluster"
    - value: "tap-mc-iterate-admin@tap-mc-iterate"
      text: "Iterate Cluster"
    - value: "tap-mc-build-admin@tap-mc-build"
      text: "Build Cluster"
    - value: "tap-full-admin@tap-full"
      text: "Single Cluster TAP"
    defaultValue: "tap-mc-iterate-admin@tap-mc-iterate"
    required: true
